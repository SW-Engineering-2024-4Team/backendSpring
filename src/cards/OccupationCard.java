package cards;

import actions.Action;
import models.Player;

public class OccupationCard extends AbstractCard {
    @Override
    public boolean execute(Player player) {
        return super.execute(player);
    }
}
