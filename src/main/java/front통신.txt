1. 프론트에서 4명이 온걸 확인한 다음 'GAME_START' 백에 보냄
    /* 프론트: start 버튼 클릭 시 서버에 메시지 보내기

            });*/




백
1. 'GAME_START'받으면 게임 로직 시작
    -> 플레이어 넘겨줌(id, )




# 일반적으로 player 클래스는 DTO가 아니라 도메인 모델의 일부로 취급됩니다.
# DTO로도 써야됨
    -도메인 객체를 DTO로 변환하려면 대부분의 경우 DTO 클래스를 만들고, 도메인 객체의 필요한 정보를 DTO에 복사하는 것이 일반적인 방법입니다.
    이러한 작업은 DTO 클래스를 작성하고, 도메인 객체에서 DTO로 데이터를 복사하는 메서드를 추가하여 수행할 수 있습니다.
    이를 보통 매퍼(Mapper) 패턴을 사용하여 처리합니다.
     -> 메모리문제..-> 풀링.. -> 어렵.. 굳이 플레이어를 전달해야할까
      -> 프론트에서도 초기설정을 하고 자원변경만 알려주는 방법?

# player id를 세션ID로 해야할지 아니면 임의로 정해도 될지..


# 방 생성: 프론트에서 만든 roomID를 이용해서 구독하면됨
# 후에 게임방 구현까지 고려하여 경로설정함
